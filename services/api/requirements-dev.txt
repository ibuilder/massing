# Test-only dependencies (not shipped in the runtime image).
#   pip install -r requirements.txt -r requirements-dev.txt
#   cd services/api && PYTHONPATH="src;../data/src" PYTHONUTF8=1 ./.venv/Scripts/python.exe run_tests.py
#   (bare `python` picks up whatever is first on PATH, NOT this venv; see run_tests.py's
#    docstring for why that fails in a way that can still report a pass)
httpx>=0.28.1          # required by starlette.testclient
pytest>=9.1.1          # optional: collects the run_tests gate
pypdf>=4.0           # NOT test-only: 7 shipped modules import it (doc_text, drawingset, pdfops,
                     # review, sheet_extract, sheet_recover, stamps). Production installs it from
                     # requirements.lock; this line only makes it available to the dev venv.
ruff>=0.16.3         # static-analysis gate (config in ruff.toml): dead code + real defects
                     # Raised from >=0.6 at v0.3.1054. A linter floor is not a normal bump:
                     # a new major flags code that has been fine for a year, and this file
                     # is covered by NO lock, so nothing would have caught it. Validated by
                     # running it: 0.16.3 installed to a temp dir (never the shared venv)
                     # and pointed at the exact scope ci.yml lints, `src/ ../data/src/` from
                     # services/api. Both 0.15.20 and 0.16.3 report `All checks passed`.
bandit>=1.9.4          # security static analysis (report-only in CI; run before shipping)
# Optional glTF Draco compression (gltf_export.export_*(draco=True)) — here rather than in
# requirements.in so the CI gate exercises the encoder without the runtime image carrying a 5 MB
# native wheel for an opt-in export. `gltf_export` imports it lazily and raises DracoUnavailable with
# an actionable message when it is absent, so the suite skips these checks loudly rather than failing.
# PINNED, not floated, so a dev machine and Linux CI run the same encoder rather than whatever each
# platform resolves to.
#
# The two lines that used to sit here said "2.0.0 publishes Windows wheels ONLY" and "1.7.0 is the
# newest with manylinux wheels". Both were FALSE when checked against PyPI on 2026-08-18: 2.0.0
# publishes 16 manylinux wheels alongside 16 Windows and 15 macOS. They were presumably true when
# written -- 2.0.0 shipped Windows-first and filled in the other platforms later -- and nothing
# re-read them afterwards. The pin was right and the prose was stale, which is the reverse of the
# usual assumption: a comment that explains a pin gets trusted BECAUSE it is specific.
# `test_gltf_compress.py` now asserts that its own message and this pin name the same version, so
# the three sources cannot drift apart silently again.
# To ship it in the API image: add to requirements.in and re-run the Lockfile workflow (it must be
# compiled in the py3.12 Linux container, and the gate fails on any drift between the two).
DracoPy==2.0.0       # Apache-2.0

# coverage: the test gate's opt-in COVERAGE=1 mode (run_tests.py) — shards + combined + xml
coverage>=7.6
