{
  "key": "commitment",
  "name": "Commitments (POs)",
  "section": "Cost",
  "icon": "▣",
  "ref_prefix": "PO",
  "title_field": "description",
  "pinnable": false,
  "fields": [
    {"name": "description", "label": "Description", "type": "text", "required": true},
    {"name": "cost_code", "label": "Cost Code", "type": "text"},
    {"name": "vendor", "label": "Vendor / Sub", "type": "text"},
    {"name": "amount", "label": "Committed Amount", "type": "number", "required": true}
  ],
  "workflow": {
    "initial": "draft",
    "states": ["draft", "executed", "closed"],
    "transitions": [
      {"from": "draft", "to": "executed", "action": "execute", "party": ["GC"]},
      {"from": "executed", "to": "closed", "action": "close", "party": ["GC"]}
    ]
  }
}
