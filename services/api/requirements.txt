# API service — BCF-shaped backend (guide §7)
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
python-multipart>=0.0.9   # file uploads (attachments)
boto3>=1.34               # MinIO/S3 object storage (range reads); lazy-imported
# psycopg[binary]>=3.1     # Postgres driver — the Docker image installs it explicitly (prod uses
#                          # Postgres); local dev runs on sqlite so it's not needed in the venv

# analysis/QA + export bridge to the data service (imported from services/data/src)
ifcopenshell>=0.8.0,<0.9
openpyxl>=3.1
numpy>=2.5.0
ifctester>=0.8
trimesh>=4.0
manifold3d>=2.0
scipy>=1.11
networkx>=3.0
shapely>=2.0
reportlab>=4.0
pyhanko>=0.25            # PAdES certificate-based PDF digital signatures (esign.py)
pypdf>=4.0               # contract/spec text extraction for the review engine (review.py)
defusedxml>=0.7          # XXE / billion-laughs hardening for untrusted CityGML parsing (citygml.py)

# optional: shared rate-limit + login-lockout counters across workers — only imported when
# AEC_REDIS_URL is set (the prod overlay sets it); harmless to omit for single-worker / dev.
redis>=5.0

# optional AI assists (Draft RFI) — only used when ANTHROPIC_API_KEY is set; lazy-imported
anthropic>=0.69

# optional E57 point-cloud import — heavy/native (libe57format); only needed to convert .e57 scans
# server-side to .xyz. Lazy-imported; GET /convert/e57/status reports availability. Uncomment to enable.
# pye57>=0.4
