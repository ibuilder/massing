{
  "name": "massing",
  "version": "0.0.0",
  "private": true,
  "description": "Standalone web BIM viewer + data platform for AEC firms (IFC-native).",
  "workspaces": [
    "apps/web",
    "packages/shared-types"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/web",
    "build": "npm run build --workspace apps/web"
  },
  "engines": {
    "node": ">=20"
  },
  "overrides": {
    "eslint": "9.39.5"
  },
  "devDependencies": {
    "eslint": "9.39.5"
  }
}
